syntax = "proto3";

// 偷个懒，就不定义request 和 response 结构了，直接应用服务端的定义。
import "gitee.com/wolferhua/go-micro-demo/demo001/rpc1/proto/srv1/srv1.proto";

// 定义新服务，
service Rpc1 {
    rpc Say(HelloRequest) returns (HelloResponse) {}
}

// protoc --proto_path=$GOPATH/src:. --micro_out=. --go_out=. rpc1.proto